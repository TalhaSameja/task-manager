import React from 'react';

function About() {
  return (
    <div className="todo-container">
      <h2 className="todo-title">About This App</h2>
      <p>This is a full-stack to-do app built using React, Express, and MongoDB. It supports adding, removing, and viewing tasks using a token-based API.</p>
    </div>
  );
}

export default About;
